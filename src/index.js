import ReactDOM from "react-dom"
import {QuoteContainer} from "./containers/quoteBox.container.jsx"
import { CSSTransition } from "react-transition-group";


ReactDOM.render(QuoteContainer, document.getElementById("root"))